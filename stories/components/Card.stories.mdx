import { Meta, Story, Canvas } from '@storybook/addon-docs'

export const Template = (args) => <div {...args} />

<Meta title="Components/Card" />

# Card

The `'card'` component provides a distinctive background, border, elevating shadow, and padding for its children.

<Canvas>
  <Story
    args={{ children: <p>Hello world!</p>, className: 'card' }}
    name="Default"
  >
    {Template.bind({})}
  </Story>
</Canvas>
